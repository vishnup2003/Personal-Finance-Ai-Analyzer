# Railway / Render ke liye correct port
server.port=${PORT:8080}

# MongoDB Atlas connection - env variable se read karega
# Pehle SPRING_DATA_MONGODB_URI try karega, nahi mila to MONGODB_URI
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:${MONGODB_URI}}
spring.data.mongodb.database=finance_ai_db
